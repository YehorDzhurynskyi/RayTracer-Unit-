Need to store on host the copy of scene to speed up the read access,
then on writing access call map/unmap functions to upload changes

Add camera rotations

Fix plane intersection functions

Fix shadows in distance
